// Developers persona: header + section ordering. No stepIcons — the developer
// TOC uses numeric section badges instead of icons.
(function () {
  var P = window.LemonadePersona;
  if (!P) return;
  P.registerPersona('developers', {
    zone: 'Meet your new AI stack.',
    zoneSubtitle: 'Lemonade takes care of every aspect of setting up and executing AI models so that you can focus on what makes your apps and agents unique.',
    label: 'Software stack'
  });
})();
