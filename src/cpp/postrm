#!/bin/bash
set -e

# Clean up systemd state on purge
if [ "$1" = "purge" ] ; then
    if [ -x "/usr/bin/deb-systemd-helper" ]; then
        deb-systemd-helper purge 'lemond.service' >/dev/null || true
        deb-systemd-helper unmask 'lemond.service' >/dev/null || true
    fi
fi

exit 0
